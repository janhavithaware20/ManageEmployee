//#include "CSVWriter.h"
/*template<typename T> void CSVWriter::DownloadData(T startt, T endt) {
	fstream myfile;
	myfile.open(fileName, ios::out | (linescount ? ios::app : ios:trunc));
	for (; startt != endt; ) {
		myfile << *start;
		if (++startt != endt) file << delimiter;
	}
	myfile << "\n";
	linescount++;
	myfile.close();
}
*/